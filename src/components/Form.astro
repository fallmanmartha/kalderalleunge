---
import Heading from "../components/Heading.astro";
import Text from "../components/Text.astro";
import Quote from "./Quote.astro";
---

<div class="relative mr-15">
  <!-- ROSA border bakom -->
  <div class="absolute bottom-15 left-15 w-full h-full bg-button-tertiary-default z-0"></div>

  <!-- SVART BOXEN -->
  <section class="relative z-10 bg-cards-primary py-20 px-10 lg:px-15" x-data="{ step: 1, showThanks: false }">
    <div class="max-w-6xl mx-auto">
      <!-- Titel -->
      <div class="mb-h2-mobile lg:mb-h2-desktop">
        <Heading htmlheading="h2" size="large" font="quote" color="secondary" weight="italic"> TILMELD DIG HER </Heading>
      </div>

      <div class="grid grid-cols-1 lg:grid-cols-12 gap-grid-gap-mobile lg:gap-grid-gap-desktop">
        <!-- Vänster text -->
        <div class="lg:col-span-5">
          <div class="lg:mb-p-desktop mb-p-mobile">
            <Text color="secondary"> Ambassadørnetværket er for alle unge i aldersgruppe 16-30 år, der interesserer sig for ligestilling. </Text>
          </div>

          <div class="lg:mb-p-desktop mb-p-mobile">
            <Text color="secondary"> Har du spørgsmål inden du tilmelder dig? Så skriv blot til projektleder Louise Kjølsen her. </Text>
          </div>

          <div class="flex items-start gap-grid-gap-small">
            <Text color="secondary"> Når du tilmelder dig som ambassadør får du info om næste ledige datoer for ambassadørworkshop. Du kan godt starte i netværket og senere deltage i en workshop. </Text>

            <div class="lg:-mt-10 -mt-0">
              <Quote color="tertiary">!</Quote>
            </div>
          </div>
        </div>

        <!-- FORM-->
        <div class="lg:col-span-7">
          <form>
            <!-- STEG 1 -->
            <div x-show="step === 1" x-cloak>
              <div class="mb-h3-mobile lg:mb-h3-desktop">
                <Heading htmlheading="h3" size="medium" color="secondary" font="heading" weight="medium"> De praktiske informationer </Heading>
              </div>

              <div class="mb-6">
                <label class="block text-text-secondary text-xl mb-p-mobile lg:mb-p-desktop"> *Navn </label>

                <div class="grid grid-cols-1 md:grid-cols-2 gap-grid-gap-small">
                  <div>
                    <label class="text-text-secondary">Fornavn</label>
                    <input required type="text" name="fornavn" placeholder="Rosa" class="w-full bg-body-primary text-text-primary p-input outline-none placeholder:text-text-placeholder focus:ring-focus-primary" />
                  </div>

                  <div>
                    <label class="text-text-secondary">Efternavn</label>
                    <input type="text" name="efternavn" placeholder="Parks" class="w-full bg-body-primary text-text-primary p-input outline-none placeholder:text-text-placeholder focus:ring-focus-primary" />
                  </div>
                </div>
              </div>

              <div class="lg:mb-p-desktop mb-p-mobile">
                <label class="text-text-secondary text-xl">*Email</label>
                <input type="email" name="email" placeholder="rosaparks@email.com" class="w-full bg-body-primary text-text-primary p-input outline-none placeholder:text-text-placeholder focus:ring-focus-primary" />
              </div>

              <div class="lg:mb-p-desktop mb-p-mobile">
                <label class="text-text-secondary text-xl">*Telefon</label>
                <input type="tel" name="telefon" placeholder="+45 00 00 00 00" class="w-full bg-body-primary text-text-primary p-input outline-none placeholder:text-text-placeholder focus:ring-focus-primary" />
              </div>
            </div>

            <!-- STEG 2 -->
            <div x-show="step === 2" x-cloak>
              <div class="mb-h3-mobile lg:mb-h3-desktop">
                <Heading htmlheading="h3" size="medium" color="secondary" font="heading" weight="medium"> Lidt om dig </Heading>
              </div>

              <div class="lg:mb-p-desktop mb-p-mobile">
                <label class="text-text-secondary text-xl">*Alder</label>
                <input type="number" name="alder" placeholder="25" class="w-full bg-body-primary text-text-primary p-input outline-none placeholder:text-text-placeholder focus:ring-focus-primary" />
              </div>

              <div class="lg:mb-p-desktop mb-p-mobile">
                <label class="text-text-secondary text-xl">Køn</label>
                <input type="text" name="koen" placeholder="Kønsidentitet" class="w-full bg-body-primary text-text-primary p-input outline-none placeholder:text-text-placeholder focus:ring-focus-primary" />
              </div>

              <div class="lg:mb-p-desktop mb-p-mobile">
                <label class="text-text-secondary text-xl">*By</label>
                <input type="text" name="by" placeholder="København" class="w-full bg-body-primary text-text-primary p-input outline-none placeholder:text-text-placeholder focus:ring-focus-primary" />
              </div>
            </div>

            <!-- STEG 3 -->
            <div x-show="step === 3" x-cloak>
              <div class="mb-h3-mobile lg:mb-h3-desktop">
                <Heading htmlheading="h3" size="medium" color="secondary" font="heading" weight="medium"> Lad os lære dig at kende </Heading>
              </div>

              <div class="lg:mb-p-desktop mb-p-mobile">
                <label class="text-text-secondary text-xl"> Hvad laver du til daglig? </label>
                <input type="text" name="hvad_laver_du" placeholder="Jeg studerer og arbejder…" class="w-full bg-body-primary text-text-primary p-input outline-none placeholder:text-text-placeholder focus:ring-focus-primary" />
              </div>

              <div class="lg:mb-p-desktop mb-p-mobile">
                <label class="text-text-secondary text-xl"> Hvorfor ønsker du at blive ambassadør? </label>
                <input type="text" name="hvorfor" placeholder="Jeg vil gerne engagere flere unge…" class="w-full bg-body-primary text-text-primary p-input outline-none placeholder:text-text-placeholder focus:ring-focus-primary" />
              </div>

              <div class="lg:mb-p-desktop mb-p-mobile">
                <label class="block text-text-secondary text-xl"> Samtykke </label>
                <label class="flex items-start gap-grid-gap-small text-text-secondary text-sm">
                  <input type="checkbox" name="samtykke" class="mt-1 rounded-full w-8 h-8 bg-text-button-primary border-2 border-white focus:ring-focus-primary cursor-pointer" />
                  <span>
                    Jeg giver hermed samtykke til at Dansk Kvindesamfund/KALDER ALLE UNGE må tage kontakt til mig ifm Dansk Kvindesamfund/KALDER ALLE UNGE aktiviteter. Jeg giver derudover samtykke til at Dansk Kvindesamfund/KALDER ALLE UNGE må
                    opbevare mine kontaktinformationer.
                  </span>
                </label>
              </div>
            </div>

            <!-- KNAPPER -->
            <div class="mt-section-mobile flex items-center justify-between">
              <div class="flex gap-grid-gap-small">
                <span class="w-4 h-4 rounded-full" :class="step === 1 ? 'bg-button-tertiary-default' : 'bg-white'"></span>
                <span class="w-4 h-4 rounded-full" :class="step === 2 ? 'bg-button-tertiary-default' : 'bg-white'"></span>
                <span class="w-4 h-4 rounded-full" :class="step === 3 ? 'bg-button-tertiary-default' : 'bg-white'"></span>
              </div>

              <div class="flex gap-grid-gap-small">
                <button type="button" x-show="step > 1" x-on:click="step--" class="px-6 md:px-8 py-3 rounded-full border-2 border-text-secondary text-text-secondary hover:rounded-none transition-all ease-in-out cursor-pointer"> Tilbage </button>
                <button
                  type="button"
                  x-show="step < 3"
                  x-on:click="step++"
                  class="px-6 md:px-8 py-3 rounded-full bg-button-primary-default hover:bg-button-primary-hover active:bg-button-primary-active text-text-button-primary hover:rounded-none transition-all ease-in-out cursor-pointer"
                >
                  Næste
                </button>
                <button
                  type="button"
                  x-show="step === 3"
                  x-on:click="showThanks = true"
                  class="px-6 md:px-8 py-3 rounded-full bg-button-primary-default hover:bg-button-primary-hover active:bg-button-primary-active text-text-button-primary hover:rounded-none transition-all ease-in-out cursor-pointer"
                >
                  Send
                </button>
              </div>
            </div>
          </form>
        </div>
      </div>
    </div>

    <!-- TAKKEBOKS -->
    <div x-show="showThanks" x-cloak class="fixed inset-0 bg-black/60 flex items-center justify-center z-50">
      <div class="bg-cards-tertiary text-text-primary p-8 md:p-10 max-w-2xl mx-4">
        <h2 class="text-2xl font-semibold mb-4">Tak for din tilmelding!</h2>
        <p class="mb-6">Vi kontakter dig snart på den email, du har angivet.</p>
        <button
          class="px-8 py-3 rounded-full bg-button-primary-default hover:bg-button-primary-hover active:bg-button-primary-active text-text-button-primary hover:rounded-none transition-all ease-in-out cursor-pointer"
          x-on:click="showThanks = false"
        >
          Luk
        </button>
      </div>
    </div>
  </section>
</div>
